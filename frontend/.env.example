# Frontend Environment Configuration
# Copy to .env.local and customize for your environment

# =============================================================================
# API Configuration
# =============================================================================

# API URL - defaults to /api which works with nginx proxy
# For development without proxy, use: http://localhost:5000/api
VITE_API_URL=/api

# =============================================================================
# Development Settings (optional)
# =============================================================================

# Development server port (default: 5173)
# VITE_DEV_PORT=5173

# Backend URL for dev server proxy (default: http://localhost:5000)
# VITE_BACKEND_URL=http://localhost:5000
